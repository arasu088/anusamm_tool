<%= form_with(model: labour_payment) do |form| %>
  <% if labour_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(labour_payment.errors.count, "error") %> prohibited this labour_payment from being saved:</h2>

      <ul>
        <% labour_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.collection_select :project_id, Project.where("is_active=1").order(:name),:id,:name, include_blank: true,:required=>true %>
  </div>

  <div class="field">
    <%= form.label :labour_type_id %>
    <%= form.collection_select :labour_type_id, LabourType.where("is_active=1").order(:name),:id,:name, include_blank: true,:required=>true %>
  </div>

  <div class="field">
    <%= form.label :labour_id %>
    <%= form.collection_select :labour_id, Labour.select("labours.id,labours.name || '-' || lt.name as name").joins("inner join labour_types lt on labours.labour_type_id=lt.id").where("labours.is_active=1").order(:name),:id,:name, include_blank: true,:required=>true %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<script>


</script>